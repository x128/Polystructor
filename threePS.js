THREE = require('lib/three.js');

var view = require('view');
//console.log(view);

view.create(view.ViewOptions.FullScreen);
view.startRendering();

//var detail = new Detail(args);
//var detail = Mushroominator.createDetail(args);
//view.add(detail);
